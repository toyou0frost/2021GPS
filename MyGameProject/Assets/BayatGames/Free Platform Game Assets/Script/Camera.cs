using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Camera : MonoBehaviour
{
    //private float maxSpeed = 5.0f;
    //Rigidbody2D rigid;
    //Player playerPos = new Player();
    //private Player cc;

    // Start is called before the first frame update
    void Start()
    {
        //rigid = GetComponent<Rigidbody2D>();
        //cc = GetComponent<Player>();
    }

    // Update is called once per frame
    void Update()
    {
        //gameObject.transform.position = new Vector3(playerPos.playerPos.x, playerPos.playerPos.y, playerPos.playerPos.z);
        //Debug.Log(playerPos.playerPos);
        //gameObject.transform.position = new Vector3(cc.playerPos.x, cc.playerPos.y, cc.playerPos.z);
        //Debug.Log(cc.playerPos);
    }
}
